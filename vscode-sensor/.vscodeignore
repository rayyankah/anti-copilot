# The extension is bundled into dist/extension.js by esbuild, so ship only that
# plus package.json. Everything else is dev-only.
.vscode-test/**
.vscode/**
.env
node_modules/**
src/**
**/*.ts
**/*.map
tsconfig.json
.gitignore
.eslintrc*
.vscodeignore
.git/**
*.vsix
